---
title: "Red team — docker workspace kind plan"
status: accepted
created: 2026-08-17
---

# Red team

Hostile review of `260817-1316-docker-workspace-kind` after first draft.

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | High | `dc-ls --all` compose-kind via daemon `working_dir` scan = Docker controller | **Accepted** — fleet stays labeled-only. Compose-kind is this-folder only. |
| 2 | High | Synthetic `dc-ls` row for a stopped compose folder breaks the container-list contract | **Accepted** — stopped → `[]`. Kind on doctor/detect. |
| 3 | Med | Manifesto rewrite in Phase 2 for a feature that may never start | **Accepted** — manifesto waits for Phase 3. Phase 2 skill = detect-only. |
| 4 | Med | TUI app-row restart via service-name guess | **Accepted** — refuse iff `stack[i].ID == rows[0].ID`. |
| 5 | Med | take-ports compose-kind stop in Phase 2 without lifecycle | **Accepted** — Phase 3 only. |
| 6 | Low | `e` on compose-kind with no app/web service | **Accepted** — `e` refuses; Enter still execs selected row. |
| 7 | Low | Phase 1 restart without mutation lock | **Rejected** — same class as existing `dc_ensure_running` `docker start`. Do not invent a lock here. |

No remaining contradictions on locks: unlabeled report-only, no fake DC labels, Phase 3 gated, no daemon UI.

Status: DONE
Summary: Five accepted locks written into plan.md + phase files. Fleet/daemon-scan path killed.
