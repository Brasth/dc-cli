/** Locked install one-liner — must match assets/branding/copy.md and README. */
export const installCommand =
  'curl -fsSL https://raw.githubusercontent.com/Brasth/dc-cli/main/install.sh | bash -s -- --with-cli';
