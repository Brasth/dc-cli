# dc-cli site

Static marketing page for [dc-cli](https://github.com/Brasth/dc-cli). Canonical URL: https://dc.brasth.com

```bash
cd site
npm install
npm run dev
npm run build
```

Deploy: `.github/workflows/pages.yml` on `site/**` pushes to `main`.
